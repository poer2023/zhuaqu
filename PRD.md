# PRD：X 内容存储池・强改写・单账号发布系统

- 版本：v1.0（MVP）
- 状态：已评审通过（按当前决策）
- 最后更新：2025-12-14

## 1. 背景与愿景

你在浏览 X（Twitter）时会遇到大量可复用的信息：观点、案例、数据、产品发布、行业动态、讨论串等。当前“收藏/点赞/复制链接”的方式难以形成可管理的素材资产，也无法高效完成“改写成自己的表达→重新发布”的闭环。

本系统的愿景是把 X 的公开内容变成“可沉淀、可编辑、可审阅、可发布、可追踪”的素材资产：  
**快速入库 → 归档到工作区/素材池 → 强改写（重写观点）→ 人工审阅 → 单账号发布（含线程/定时/队列）→ 结果回写与复盘。**

> 合规原则（必须遵守）：仅处理你拥有权利或获得授权的内容；遵守 X 平台规则与当地法律法规；默认启用发布前确认与审计日志。

## 2. 目标（Goals）

- G1：用最低成本把任意推文 URL 入库到指定工作区/素材池，并可追踪处理状态与失败原因。
- G2：素材池支持检索/筛选/去重/多选批量操作，形成可复用的内容资产库。
- G3：支持对单条或多条素材进行**强改写（重写观点、结构、表述）**，且具备版本管理与人工审阅流。
- G4：连接**单个** X 账号，可靠完成发布：单条/线程、定时、队列、失败重试、发布结果回写。
- G5（v1.1）：浏览器扩展在时间线上覆盖勾选框，实现“选择模式”多选入库。
- G6（v1.2）：对点赞/收藏（Bookmarks）进行快速批量入库（任务化、可断点续跑、可重试）。

## 3. 非目标（Non-goals）

- 不提供任何绕过访问控制/反爬/风控的具体手段；不承诺抓取私密/受限内容。
- 不做“全自动无人审核的大规模搬运发布”。v1.0 默认**强制人工确认后发布**（可配置但需要风险提示与权限）。
- v1.0 不做多账号矩阵发布；仅单账号。
- v1.0 不做复杂数据分析与增长归因（仅提供基础发布记录与少量指标）。

## 4. 已确定的关键决策

- 交互形态：先做**纯后台**（粘贴 URL）验证闭环；后续再做浏览器扩展。
- 多工作区/多主题素材池：必须支持（用于项目/主题隔离）。
- 改写方式：以**强改写（重写观点）**为核心，输出更符合你账号定位的内容。
- 发布：仅支持连接一个 X 账号（按工作区绑定或全局绑定，见 Open Questions）。
- 扩展（v1.1）选择模式：在时间线推文卡片上覆盖勾选框，多选批量入库。

## 5. 用户画像与使用场景

### 5.1 用户画像

- Owner（你）：采集、编辑、发布一体；对效率与可控性敏感；需要审计与可追溯。
-（后续）Editor/Reviewer：采集与改写分工、审核与发布分离。

### 5.2 核心场景

- S1（v1.0）：复制 1~N 条推文 URL → 入库到某工作区/素材池 → 多选强改写 → 审阅 → 发布 → 回写链接。
- S2（v1.1）：浏览时间线开启选择模式 → 勾选多条 → 一键入库到指定素材池 → 回后台改写发布。
- S3（v1.2）：对 Likes/Bookmarks 创建批量同步任务 → 跑完入库 → 筛选后批量强改写发布。

## 6. 信息架构（IA）

### 6.1 全局

- 顶栏：工作区切换器、全局搜索（可选）、用户菜单
- 侧边栏：素材池 / 入库 / 改写工作台 / 发布中心 / 同步任务（v1.2）/ 账号与设置 / 审计日志

### 6.2 工作区（Workspace）

每个工作区拥有：
- 素材池（Pools）
- 标签体系（Tags）
- 强改写预设（Rewrite Presets）
- 默认发布策略（Publish Defaults）
- 黑名单与拦截规则（可选）
- 绑定账号配置（单账号：按工作区或全局）

## 7. v1.0（MVP）功能范围（P0）

### 7.1 页面与交互规格

#### 7.1.1 入库页（粘贴 URL）

**目标**：把推文 URL 快速变成“条目”，进入素材池可编辑可发布。

**主要组件**
- 工作区选择（全局已有则只显示当前工作区）
- 目标素材池选择（必选）
- URL 输入框（多行，每行一个 URL；支持粘贴自动清洗）
- 入库选项：
  - 线程策略：仅当前推文 / 自动展开线程（若可获取）
  - 引用策略：不跟随 / 跟随引用推文（可选）
  - 媒体策略：仅保存链接（默认）/ **下载并持久化到本地**（可选，需配置存储）
    - 当选择“下载并持久化”且素材含视频时：后台先提取视频直链并下载到本地；下载完成后才允许用户点击下载/进入视频 AI 流程（不做“直链转发下载”）
    - 下载落盘后保存：`local_path`、`sha256`、`bytes`、`downloaded_at`；失败则写入 `error` 并把 `capture_status=failed`（条目仍保留，便于重试/改用仅链接模式）
  - 去重策略：按 tweet_id（默认）/ 额外按内容指纹（可选）
- 标签（可多选/新建）
- 备注（可选）
- 提交按钮（创建入库批次）
- 入库批次面板：显示本次批次进度与结果（成功/去重/失败列表）

**状态与校验**
- URL 校验：支持 `x.com/.../status/<id>`、`twitter.com/.../status/<id>`；非法行标红并可一键移除。
- 幂等：重复 URL/tweet_id 不重复入库；在结果里显示“已存在条目并已关联来源/标签”的反馈。
- 失败提示：展示错误码、可复制详情、单条重试/批量重试。

#### 7.1.2 素材池列表页（核心工作台）

**目标**：管理条目资产，支持筛选、预览、多选批量操作。

**列表字段（表格/卡片二选一，v1.0 先表格）**
- 选框、多选操作条
- 作者（handle）、原文摘要、媒体标识（图/视频/无）、标签、入库来源、入库时间
- 状态：
  - capture_status：queued/fetching/ready/failed
  - rewrite_status：none/drafting/generated/approved/rework
  - publish_status：not_published/queued/published/failed

**筛选与搜索**
- 素材池、标签、作者、媒体类型、状态组合、时间范围（入库/发布）
- 关键词搜索（原文/改写稿/备注）

**批量操作（多选后显示）**
- 加标签 / 移动素材池
- 创建改写批次（强改写）
- 加入发布队列（必须已存在 approved 的改写版本）
- 归档/删除（危险操作二次确认）

**详情抽屉**
- 原文、来源链接、作者、时间、线程结构（若有）
- 媒体预览
  - 图片：缩略图 + 原图链接
  - 视频：
    - 未落盘：显示来源链接/直链（可复制），并展示下载状态（queued/fetching/failed）
    - 已落盘：支持本地预览（可选）+ **下载按钮（从你后台下载本地文件）** + 直链复制
- 改写版本列表（版本号、状态、时间、操作者）
- 日志（入库/改写/发布事件）
- 发布映射（已发布 tweet 链接、发布时间、回执摘要）

#### 7.1.3 改写工作台（批次）

**目标**：对选中条目进行强改写，并完成“生成→审阅→通过”。

**批次创建入口**
- 从素材池多选 → “创建改写批次”
- 批次基本信息：名称（默认自动生成）、目标素材池（可选）、标签策略（沿用/新增）

**强改写配置（Rewrite Preset + 参数）**
- 目标账号定位（品牌/人设/行业）
- 受众与语气（专业/通俗/犀利/温和等）
- 观点立场（支持选择“同意/反对/中立/对比分析/补充视角”）
- 输出结构：
  - 单条推文（短）
  - 多条线程（长，输出 N 条，每条独立可编辑）
  - “钩子/结论/论据/行动建议/CTA”开关
- 事实与风险提示：
  - 是否要求“列出需核查点”（默认开）
  - 是否强制附来源链接/署名（工作区默认策略可控）
- 语言：中文为主（可选中英双语）

**生成与审阅**
- 批量生成进度：queued/running/done/partial_failed
- 单条审阅界面：左原文、右改写稿（可编辑）；按钮：通过（approved）/ 需要重写（rework）/ 丢弃（rejected）/ 再生成（regen）
- 校验提示：字数、链接数量、敏感词、重复度（与原文相似度过高提示）

**视频 → 线程稿（Gemini）**
- 前置条件：该条目存在已落盘的视频文件（下载完成）
- 动作：点击“视频生成线程稿”→ 后台把本地视频提交给 Gemini 分析 → 产出结构化线程草稿（JSON）
- 输出要求：偏“可直接改写成推文线程”的结构化稿（线程标题、每条推文文本、hashtags、需核查点、来源链接）
- 产物：写入 `RewriteVersion(output_format=thread, input_mode=video)`；进入同一套“审阅→通过→发布”流程（默认仍需人工确认）

**产出**
- approved 的版本可直接进入发布向导；未 approved 不可发布（默认规则）。

#### 7.1.4 发布中心（队列 + 向导）

**目标**：可靠发布并回写；支持定时与失败恢复。

**发布向导**
- 选择发布内容：来自“改写批次”或“素材池多选（已 approved）”
- 发布模式：
  - 单条发布
  - 线程发布（按输出顺序自动 reply 串联）
- 定时：立即 / 定时（选择时间）
- 发布前校验：
  - 必须存在 approved 改写稿
  - 字数、媒体策略、链接策略
  - 合规确认勾选（默认必选）
- 确认后创建 PublishJob（进入队列）

**队列列表**
- 状态：queued/running/succeeded/failed/partial
- 展示：计划时间、开始/结束时间、失败原因、重试次数
- 操作：暂停队列、取消未执行、单任务重试、失败项批量重试

**回写**
- 保存发布结果：tweet_id、url、发布时间、请求回执摘要、线程映射（每条对应关系）
- 素材条目 publish_status 更新为 published/failed，并关联 publish_job_id。

#### 7.1.5 账号与设置（v1.0 仅单账号）

**账号连接**
- 连接/断开 X 账号（OAuth / Token，具体实现视你发布方案而定）
- 显示：账号名、权限范围、连接时间、是否有效

**工作区设置**
- 素材池管理：新增/重命名/归档
- 标签管理：新增/合并/删除（删除需确认）
- 默认策略：
  - 默认素材池
  - 默认“是否附来源链接/署名”
  - 默认改写预设
  - 默认媒体策略（仅链接/持久化）
- 黑名单（可选）：作者 handle、关键词拦截（入库/发布前提示）

### 7.2 功能需求清单（P0）

**入库**
- P0-ING-01：支持批量粘贴 URL 入库；自动解析 tweet_id；去重。
- P0-ING-02：入库批次可视化；失败可重试；错误码与可复制详情。
- P0-ING-03：条目保存 raw_json 快照；保存来源与入库参数快照。
- P0-ING-04：当素材含视频且 `media_mode=download` 时：提取直链并后台落盘；落盘完成后提供“从后台下载”接口；失败可重试并保留失败原因。

**素材池**
- P0-POOL-01：多工作区、多素材池；条目归属素材池；支持移动。
- P0-POOL-02：筛选/搜索/分页；详情抽屉预览与编辑（备注、标签）。
- P0-POOL-03：多选批量操作：打标签、移动、创建改写批次、加入发布（受规则限制）、删除/归档。

**强改写**
- P0-RW-01：改写批次创建、参数配置、批量生成、逐条审阅。
- P0-RW-02：版本管理：生成/再生成/手改均生成版本；保留参数摘要与审计。
- P0-RW-03：发布门槛：默认仅允许 approved 版本进入发布。
- P0-RW-04：视频条目支持“视频 → 线程稿（Gemini）”并写入版本，进入同一套审阅/通过/发布流。

**发布**
- P0-PUB-01：单账号连接；单条/线程发布；定时队列。
- P0-PUB-02：失败重试与队列控制（暂停/取消/重试）；回写发布链接与映射。
- P0-PUB-03：发布前合规确认（勾选）+ 审计日志记录。

**审计与日志**
- P0-AUD-01：记录入库/改写/发布关键事件；按条目与批次查看。

## 8. v1.1（P1）浏览器扩展：时间线“选择模式”覆盖勾选框

> v1.1 的目标是把“发现→入库”的操作成本降到最低，但仍保持后台作为单一事实源（SSOT）。

### 8.1 扩展交互

- 在推文卡片右上角注入“入库”按钮（单条快捷）
- 增加“选择模式”开关：
  - 开启后：每条卡片出现勾选框（覆盖在卡片左上角）；底部浮动操作条显示已选数量
  - 底部操作条：选择工作区/素材池、快捷标签、提交入库、清空选择、查看提交进度
- 状态展示：已入库/处理中/失败（可重试）

### 8.2 扩展与后台联动

- 扩展仅负责采集 tweet_id/url 并提交后台创建入库批次；不在扩展侧保存发布凭证。
- 扩展需要登录态（短期 token）；token 失效则提示重新登录。

## 9. v1.2（P2）点赞/收藏（Bookmarks）批量入库

### 9.1 两种实现路径（择一或并行）

- 路径 A（推荐，合规）：通过官方/授权接口在用户授权范围内获取 Likes/Bookmarks 列表。
- 路径 B（辅助）：扩展在 Likes/Bookmarks 页面“采集可见/采集到 N”，将 tweet_id 列表提交后台任务化入库。

### 9.2 同步任务（SyncJob）

- 创建任务：选择来源（Likes/Bookmarks）、范围（数量/时间）、过滤（是否含媒体/作者黑名单）、线程策略。
- 任务进度：已发现/已提交/已入库/失败/去重。
- 控制：暂停/继续/取消；失败项单独重试；断点续跑（不重复处理已成功条目）。

## 10. 数据模型与状态机（v1.0 必需）

### 10.1 核心实体（简化）

- Workspace：`id, name, default_pool_id, settings, created_at`
- Pool：`id, workspace_id, name, created_at`
- ContentItem：
  - 标识：`id, workspace_id, pool_id, source_id(tweet_id), source_url`
  - 内容：`author_handle, text_original, lang, raw_json`
  - 媒体：`media[]`（至少记录 source_url/type；是否持久化视策略）
  - 状态：`capture_status, rewrite_status, publish_status`
  - 管理：`tags[], notes, created_at, updated_at`
- RewriteVersion：`id, content_item_id, preset_id, params_snapshot, output (single/thread), status, created_at`
- PublishJob：`id, workspace_id, mode, schedule_at, status, result_map, error, created_at`
- IngestJob：`id, workspace_id, pool_id, inputs, options, status, progress, error, created_at`

### 10.2 状态机

- capture_status：`queued → fetching → ready | failed`
- rewrite_status：`none → drafting → generated → approved | rework | rejected`
- publish_status：`not_published → queued → published | failed`

## 11. 接口契约（高层，不含采集实现细节）

> 采集核心（ingest engine）视为黑盒，但对外必须提供稳定的任务、状态、错误码与重试语义。

- `POST /api/ingest/jobs`
  - body：`workspace_id, pool_id, urls[], tags[], notes, options{thread_mode, quote_mode, media_mode}`
  - resp：`job_id, deduped_count, accepted_count`
- `GET /api/ingest/jobs/:job_id`
  - resp：`status, progress{total, succeeded, failed, deduped}, failures[]`
- `GET /api/content-items/:item_id/media/:media_index/download`
  - 语义：当且仅当后台已落盘完成时返回视频文件下载；未落盘返回冲突/错误
- `POST /api/content-items/:item_id/video-to-thread`
  - 语义：把本地视频提交 Gemini，生成“可发线程”的结构化稿并写入 RewriteVersion
- `GET /api/pools/items`
  - query：`workspace_id, pool_id, q, tags, author, status, media_type, page`
  - resp：`items[], page_info`
- `POST /api/rewrite/batches`
  - body：`workspace_id, item_ids[], preset_id, params`
  - resp：`batch_id`
- `GET /api/rewrite/batches/:batch_id`
  - resp：`status, items[{item_id, versions[]}]`
- `POST /api/publish/jobs`
  - body：`workspace_id, mode, payload, schedule_at`
  - resp：`publish_job_id`
- `GET /api/publish/jobs/:publish_job_id`
  - resp：`status, result_map, error`

（v1.2）同步任务：
- `POST /api/sync/jobs`、`GET /api/sync/jobs/:id`

## 12. 安全、隐私与权限

- v1.0：单用户即可；后续扩展角色（Owner/Editor/Reviewer）。
- 发布凭证（token/secret）只能保存在服务端加密存储；前端/扩展不得长期持有可发布密钥。
- 危险操作二次确认：删除条目、批量删除、批量发布、清空队列、导出数据。
- 数据保留与删除：支持归档/删除；支持按作者/来源批量移除以应对合规需求。
- 默认启用审计日志；对每次发布记录“确认勾选”。

## 13. 非功能需求（NFR）

- 可用性：核心流程（入库/改写/发布）具备可重试与幂等语义。
- 性能：素材池列表 1 万条以内分页加载 < 500ms（不含网络）；搜索可接受 1~2s（视实现）。
- 可靠性：发布队列需具备重试、退避、限速与断点恢复。
- 可观测性：每个 Job 有唯一 trace_id；支持导出失败样本与错误码分布。

## 14. 风险与合规

- 版权与不当转载风险：默认提示与发布前确认；可配置“自动附来源链接/署名”；支持黑名单作者与关键词拦截。
- 平台政策风险：不提供绕过风控的产品功能；遵守 API/授权边界；提供一键暂停发布队列。
- 数据结构变动风险：raw_json 快照 + 可重放解析；采集引擎版本化；失败快速定位与回滚。

## 15. 里程碑（建议）

- M0（1 周）：信息架构 + 页面原型（低保真）+ 接口契约定稿；v1.0 技术选型确定。
- M1（2~3 周）：v1.0 闭环
  - 入库页（URL 批量）+ IngestJob 状态
  - 素材池列表/详情 + 多工作区/素材池
  - 改写批次（生成/版本/审阅）
  - 单账号发布（单条/线程/定时队列）+ 回写 + 日志
- M2（1~2 周）：体验与稳定性
  - 更完善筛选/搜索、批量操作、失败重试体验
  - 风险提示与默认策略完善
- M3（2 周）：v1.1 浏览器扩展（选择模式覆盖勾选框）
- M4（2~3 周）：v1.2 Likes/Bookmarks 批量同步任务

## 16. v1.0 验收标准（Acceptance Criteria）

- AC1：粘贴 10 条有效 URL 创建入库批次，UI 可见进度与去重；成功条目出现在指定素材池且可打开详情。
- AC2：素材池多选 5 条创建强改写批次；每条至少生成 1 个版本；支持再生成与通过/驳回。
- AC3：连接单账号后可发布单条与线程；发布完成后条目显示发布链接与发布时间；失败可重试且不重复发布同一版本（幂等策略需明确）。
- AC4：工作区隔离生效：A 工作区条目在 B 工作区不可见；预设与默认策略按工作区生效。
- AC5：审计日志可按条目追踪入库→改写→发布全链路，并包含关键参数摘要。

## 17. Open Questions（需要你确认的最后 2 个点）

1) 单账号绑定粒度：是“全局一个账号”，还是“每个工作区一个账号（但总数仍为 1）”？  
2) 媒体策略 v1.0 取舍：默认仅保存媒体链接，还是对“含视频的条目”默认后台落盘到本地（便于下载与 Gemini 分析）？
